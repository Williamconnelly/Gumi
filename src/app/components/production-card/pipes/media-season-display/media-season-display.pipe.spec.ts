import { MediaSeasonDisplayPipe } from './media-season-display.pipe';

describe('MediaSeasonDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaSeasonDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
